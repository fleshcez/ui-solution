import { uiFrameworkApiInterface } from './ui-framework-api-interface';

describe('uifFrameworkApiInterface', () => {
  it('should work', () => {
    expect(uiFrameworkApiInterface()).toEqual('ui-framework-api-interface');
  });
});
